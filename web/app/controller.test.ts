import {describe,it,expect} from 'vitest'; import fs from 'node:fs'; import {LocalMatch} from './controller'; import type {Policy} from '../bot/difficult';
const policy=JSON.parse(fs.readFileSync('public/policies/80_20_difficult.policy.json','utf8')) as Policy; const rng={next:()=>.1};
describe('local controller',()=>{it('samples bot before human lock',()=>{const m=new LocalMatch(policy,'B',rng);expect(m.hasPrivateBotCommit).toBe(true)});it('finishes against legal human choices',()=>{const m=new LocalMatch(policy,'B',rng);m.commitHuman(1);m.commitHuman(1);m.commitHuman([20,0,0]);expect(m.game.phase).toBe('FINISHED');expect(m.game.result).toBeTruthy();});it('fuzzes arbitrary legal human sequences',()=>{for(let i=1;i<=30;i++){const m=new LocalMatch(policy,'B',rng);const x=i%21;m.commitHuman(i%79||1);m.commitHuman(1);m.commitHuman([x,20-x,0]);expect(m.game.phase).toBe('FINISHED')}});});
