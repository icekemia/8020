import {Game,Player,Fill,commitAction,createGame} from '../game/core'; import {Policy,RandomSource,difficultFill,difficultSplit1,difficultSplit2} from '../bot/difficult';
export class LocalMatch {private state:Game=createGame(); private botPending:number|Fill|undefined; constructor(private readonly policy:Policy,private readonly bot:Player,private readonly rng:RandomSource) {this.chooseBot()} get game(){return this.state} get hasPrivateBotCommit(){return this.botPending!==undefined} private chooseBot(){const h=this.bot==='A'?'B':'A';if(this.state.phase==='SPLIT_1_COMMIT')this.botPending=difficultSplit1(this.policy,this.bot,this.rng);else if(this.state.phase==='SPLIT_2_COMMIT')this.botPending=difficultSplit2(this.policy,this.state.split.A[0],this.state.split.B[0],this.bot,this.rng);else if(this.state.phase==='FILL_COMMIT'){const d=this.state.split.A.map((x,i)=>x-this.state.split.B[i]);this.botPending=difficultFill(this.policy,d,this.bot,this.rng)}} commitHuman(action:number|Fill){const h=this.bot==='A'?'B':'A';let next=commitAction(this.state,h,action);if(next.pending[h]!==undefined&&this.botPending!==undefined)next=commitAction(next,this.bot,this.botPending);this.state=next;this.botPending=undefined;if(this.state.phase!=='FINISHED')this.chooseBot();return this.state}}
